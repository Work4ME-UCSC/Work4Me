class Review {
  constructor(id, name, avatar, rate, review) {
    this.id = id;
    this.name = name;
    this.avatar = avatar;
    this.rate = rate;
    this.review = review;
  }
}

export default Review;
